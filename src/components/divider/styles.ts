const common = { height: 2, margin: '16px 0' };

export const primary = {
    ...common,
    borderBottom: '1px solid var(--brown)',
    borderTop: '1px solid var(--brown)',
};

export const secondary = {
    ...common,
    borderBottom: '1px solid var(--white)',
    borderTop: '1px solid var(--white)',
};