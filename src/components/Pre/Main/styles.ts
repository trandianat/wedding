import { css } from '@emotion/react';

export const nav = css({
    backgroundColor: '#224331',
    padding: 24,
    textAlign: 'center',
    a: {
        color: 'white',
        padding: 24,
        textDecoration: 'none',
    }
});